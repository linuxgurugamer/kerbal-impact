KERBALCHANGELOG
{
	showChangelog = True
	modName = Impact!
	license = MIT
	author = tomforwood, Linuxgurugamer
 
 	VERSION
	{
		version = 1.9.0
		change = Updated version file to use github for version info
		change = Copied webpage to README.md
		change = Removed MiniAVC.dll
		change = Updated for 1.12.x
		change = Deprecated old parts
		change = Created 2 new parts to replace the old parts
		change = Added InstallChecker
		change = Added status text for the Seismometer and Spectormeter
		change = Added stock seismicScan experiment to the Seismometer
		change = Updated description of the Spectrometer to indicate it only works in orbit
		type = update
	}

	VERSION
	{
		version = 1.9.1
		change = Added new field to Spectrometer module: situationMask
		change = Added code which checks the situationMask of the part against the situation the vessel is in to verify vessel is in correct situation for that part (needed for new part)
		change = Added code to deploy/retract the movable parts on a spectrograph
		change = Thanks to @JadeOfMaar for the new high-orbit Spectrometer sci-impact-spectrometers3

		type = add

	}
	VERSION
	{
		version = 1.9.1.1
		change = Made experiments rerunnable, so you can run multiple impacts without needing multiple science instruments
		change = Fixed issue with the sci-impact-spectrometers3 not getting data

		type = fix

	}
}